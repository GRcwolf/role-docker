# Specifies, wheter the compose plugin should be installed.
docker_install_compose_plugin: no
# The system architecture used for adding the repo.
docker_repo_arch: amd64
# Specified if the docker service should be enabled.
docker_service_enabled: yes
